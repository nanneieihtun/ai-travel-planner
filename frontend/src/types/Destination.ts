export interface Destination {
  id: number;
  city: string;
  country: string;
  countryCode: string;
  flag: string;
  description: string;
  imageUrl: string;
  minDays: number;
  maxDays: number;
}